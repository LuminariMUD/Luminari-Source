# Pre-commit hooks for LuminariMUD
# See https://pre-commit.com for more information

repos:
  # clang-format for C/C++ formatting
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.8
    hooks:
      - id: clang-format
        types_or: [c, c++]
        args: [--style=file]
        exclude: ^src/(olc/genolc\.c|utils\.h)$

  # General file hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      # help.hlp is generated data (hedit export), and the inherited license
      # is a byte-identical legal archive. Mutating hygiene hooks skip both.
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
        exclude: ^(lib/text/help/help\.hlp|docs/legal/CIRCLEMUD_DIKUMUD_LICENSE\.txt)$
      - id: end-of-file-fixer
        exclude: ^(lib/text/help/help\.hlp|docs/legal/CIRCLEMUD_DIKUMUD_LICENSE\.txt)$
      - id: check-yaml
      - id: check-added-large-files
        args: [--maxkb=500]
        exclude: ^lib/text/help/help\.hlp$
      - id: check-merge-conflict
      - id: mixed-line-ending
        args: [--fix=lf]
        exclude: ^docs/legal/CIRCLEMUD_DIKUMUD_LICENSE\.txt$

  # Local hooks for project-specific checks
  - repo: local
    hooks:
      # Build verification (pre-push only)
      - id: check-build
        name: Verify build compiles
        entry: bash -c 'make -j$(nproc) -q 2>/dev/null || echo "Build check skipped"'
        language: system
        pass_filenames: false
        stages: [pre-push]
